# FAQ RAG Chatbot

Python project for a customer-support FAQ chatbot based on Retrieval-Augmented Generation (RAG).

The application will process a plain-text FAQ document, divide it into semantic chunks, generate embeddings, retrieve relevant information through vector similarity search, and generate grounded answers with a strict JSON output contract.

## Project status

Project initialization.